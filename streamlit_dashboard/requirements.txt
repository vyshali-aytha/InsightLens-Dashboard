# Merged requirements: streamlit_dashboard + payment_failure_prediction (existing
# dashboard/ML deps) + project/ Flask API (pipeline_dashboard integration).
# Duplicates removed; where versions differed, the higher/more permissive
# floor was kept. Also covers sales_forecast's needs (pandas, numpy, plotly),
# which were already satisfied by the shared/ML section below.

# --- Dashboard (streamlit_dashboard) ---
streamlit>=1.35
requests>=2.31
plotly>=5.20
altair>=5.0

# --- Shared / ML (payment_failure_prediction + project) ---
pandas>=2.0
numpy>=1.24
scikit-learn>=1.4
scipy>=1.10
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
psycopg2-binary>=2.9
sqlalchemy>=2.0
imbalanced-learn>=0.11
openpyxl>=3.1

# --- payment_failure_prediction API service ---
fastapi>=0.110
uvicorn>=0.29

# --- project Flask API ---
flask>=3.0
